<form method="post" action="{{ path('type_voie_delete', {'IDTypeVoie': type_voie.IDTypeVoie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_voie.IDTypeVoie) }}">
	<button class="btn">Delete</button>
</form>

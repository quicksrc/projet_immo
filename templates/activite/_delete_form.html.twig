<form method="post" action="{{ path('activite_delete', {'IDActivite': activite.IDActivite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite.IDActivite) }}">
	<button class="btn">Delete</button>
</form>

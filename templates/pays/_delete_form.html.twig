<form method="post" action="{{ path('pays_delete', {'IDPays': pay.IDPays}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pay.IDPays) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>

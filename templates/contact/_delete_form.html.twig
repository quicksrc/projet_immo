<form method="post" action="{{ path('contact_delete', {'IDContact': contact.IDContact}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.IDContact) }}">
	<button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('opportunite_delete', {'IDOpportunite': opportunite.IDOpportunite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opportunite.IDOpportunite) }}">
	<button class="btn">Delete</button>
</form>

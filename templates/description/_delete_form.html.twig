<form method="post" action="{{ path('description_delete', {'IDDescription': description.IDDescription}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ description.IDDescription) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>

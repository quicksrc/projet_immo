<form method="post" action="{{ path('description_delete', {'IDDescription': description.IDDescription}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ description.IDDescription) }}">
	<button class="btn">Delete</button>
</form>

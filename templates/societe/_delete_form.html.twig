<form method="post" action="{{ path('societe_delete', {'IDSociete': societe.IDSociete}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ societe.IDSociete) }}">
	<button class="btn">Delete</button>
</form>

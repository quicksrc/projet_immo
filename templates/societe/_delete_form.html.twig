<form method="post" class="float-end ms-1" action="{{ path('societe_delete', {'IDSociete': societe.IDSociete}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ societe.IDSociete) }}">
	<button class="btn btn-danger no-print">Supprimer</button>
</form>

<form method="post" action="{{ path('immeuble_delete', {'IDImmeuble': immeuble.IDImmeuble}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ immeuble.IDImmeuble) }}">
	<button class="btn">Delete</button>
</form>
